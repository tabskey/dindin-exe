# Chave de assinatura do JWT — copie este arquivo para .env e gere um valor forte.
# O docker compose injeta no container da API; é lida como a config "Jwt:Key".
Jwt__Key=replace-with-a-random-secret-of-at-least-32-chars
